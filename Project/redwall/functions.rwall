func
apply(f, L, size)
{
    var x = L[0];
    while (x != L[size])
    {
        f(L[x]);
        var x = L[x + 1];
    }
}

func double(x)
{
    return x * 2;
}

var a = apply(double, [1, 2, 3], 3);